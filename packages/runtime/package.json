{
  "name": "@aif/runtime",
  "version": "0.1.0",
  "description": "Runtime/provider abstraction and registry for AIF Handoff",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "lint": "eslint \"src/**/*.ts\"",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aif/shared": "*",
    "@anthropic-ai/claude-agent-sdk": "0.2.90"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
