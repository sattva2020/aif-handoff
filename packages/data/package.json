{
  "name": "@aif/data",
  "version": "0.1.0",
  "description": "Centralized data-access layer for AIF Handoff",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests --configLoader runner",
    "coverage": "vitest run --coverage --passWithNoTests",
    "lint": "eslint \"src/**/*.ts\"",
    "test:watch": "vitest --configLoader runner"
  },
  "dependencies": {
    "@aif/shared": "*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
