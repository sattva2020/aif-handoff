{
  "name": "@aif/agent",
  "version": "0.1.0",
  "description": "AI coordinator and subagents for AIF Handoff",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.ts",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "lint": "eslint \"src/**/*.ts\"",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aif/data": "*",
    "@aif/runtime": "*",
    "@aif/shared": "*"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
